<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>PCs and BROTHERS - Login & Register</title>
<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Arial', sans-serif;
    background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #fff;
}

.container {
    width: 100%;
    max-width: 500px;
    padding: 20px;
}

.auth-form {
    background: linear-gradient(135deg, rgba(30, 50, 100, 0.95), rgba(15, 25, 60, 0.95));
    border: 2px solid #FF4444;
    border-radius: 15px;
    padding: 40px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5), 0 0 30px rgba(255, 68, 68, 0.2);
    backdrop-filter: blur(10px);
}

.form-title {
    font-size: 2em;
    text-align: center;
    margin-bottom: 30px;
    color: #00FF00;
    text-shadow: 0 0 10px rgba(0, 255, 0, 0.5);
}

.form-group {
    margin-bottom: 20px;
    position: relative;
}

.form-group label {
    display: block;
    margin-bottom: 8px;
    font-weight: bold;
    color: #ddd;
    font-size: 0.9em;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.form-group input {
    width: 100%;
    padding: 12px 15px;
    border: 2px solid #00FF00;
    border-radius: 8px;
    background: rgba(0, 0, 0, 0.3);
    color: #fff;
    font-size: 1em;
    transition: all 0.3s ease;
}

.form-group input:focus {
    outline: none;
    border-color: #FF4444;
    box-shadow: 0 0 15px rgba(255, 68, 68, 0.5);
    background: rgba(255, 68, 68, 0.1);
}

.form-group input::placeholder {
    color: #888;
}

.password-toggle {
    position: absolute;
    right: 15px;
    top: 40px;
    cursor: pointer;
    color: #00FF00;
    font-size: 1.2em;
    transition: all 0.3s ease;
}

.password-toggle:hover {
    color: #FF4444;
}

.btn-submit {
    width: 100%;
    padding: 13px;
    border: none;
    border-radius: 8px;
    font-size: 1.1em;
    font-weight: bold;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-top: 20px;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.btn-login {
    background: linear-gradient(135deg, #FF4444, #FF2222);
    color: white;
    box-shadow: 0 5px 15px rgba(255, 68, 68, 0.3);
}

.btn-login:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(255, 68, 68, 0.5);
}

.btn-register {
    background: linear-gradient(135deg, #00FF00, #00DD00);
    color: #000;
    box-shadow: 0 5px 15px rgba(0, 255, 0, 0.3);
}

.btn-register:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0, 255, 0, 0.5);
}

.btn-submit:active {
    transform: translateY(0);
}

.form-toggle {
    text-align: center;
    margin-top: 20px;
    color: #ccc;
}

.form-toggle a {
    color: #00FF00;
    cursor: pointer;
    text-decoration: none;
    font-weight: bold;
    transition: all 0.3s ease;
}

.form-toggle a:hover {
    color: #FF4444;
    text-shadow: 0 0 10px rgba(255, 68, 68, 0.5);
}

#loginForm, #registerForm {
    animation: slideIn 0.5s ease;
}

@keyframes slideIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.error-message {
    color: #FF4444;
    font-size: 0.85em;
    margin-top: 5px;
    display: none;
}

.error-message.show {
    display: block;
}

.success-message {
    background: linear-gradient(135deg, rgba(0, 255, 0, 0.2), rgba(0, 200, 0, 0.2));
    border: 2px solid #00FF00;
    border-radius: 8px;
    padding: 15px;
    margin-bottom: 20px;
    color: #00FF00;
    text-align: center;
    display: none;
    animation: slideIn 0.5s ease;
}

.success-message.show {
    display: block;
}

.logo {
    text-align: center;
    font-size: 2.5em;
    margin-bottom: 20px;
}

.info-text {
    text-align: center;
    font-size: 0.85em;
    color: #aaa;
    margin-top: 15px;
    line-height: 1.4;
}

@media (max-width: 600px) {
    .auth-form {
        padding: 30px 20px;
    }
    .form-title {
        font-size: 1.5em;
    }
}
</style>
</head>
<body>
<div class="container">
    <div class="auth-form">
        <div class="logo">🎮 PCs and BROTHERS 🎮</div>
        <div id="successMessage" class="success-message"></div>
        
        <!-- LOGIN FORM -->
        <form id="loginForm">
            <h2 class="form-title">LOGIN</h2>
            <div class="form-group">
                <label>E-MAIL</label>
                <input type="email" id="loginEmail" placeholder="deine@email.com" required>
                <div class="error-message" id="loginEmailError"></div>
            </div>
            <div class="form-group">
                <label>PASSWORT</label>
                <input type="password" id="loginPassword" placeholder="••••••••" required>
                <span class="password-toggle" onclick="togglePassword('loginPassword')">👁️</span>
                <div class="error-message" id="loginPasswordError"></div>
            </div>
            <button type="submit" class="btn-submit btn-login">LOGIN</button>
            <div class="form-toggle">
                Kein Account? <a onclick="switchToRegister()">Jetzt registrieren</a>
            </div>
        </form>
        
        <!-- REGISTER FORM -->
        <form id="registerForm" style="display: none;">
            <h2 class="form-title">REGISTRIEREN</h2>
            <div class="form-group">
                <label>BENUTZERNAME</label>
                <input type="text" id="registerUsername" placeholder="dein_benutzername" required>
                <div class="error-message" id="registerUsernameError"></div>
            </div>
            <div class="form-group">
                <label>E-MAIL</label>
                <input type="email" id="registerEmail" placeholder="deine@email.com" required>
                <div class="error-message" id="registerEmailError"></div>
            </div>
            <div class="form-group">
                <label>PASSWORT</label>
                <input type="password" id="registerPassword" placeholder="••••••••" required>
                <span class="password-toggle" onclick="togglePassword('registerPassword')">👁️</span>
                <div class="error-message" id="registerPasswordError"></div>
            </div>
            <div class="form-group">
                <label>PASSWORT BESTÄTIGEN</label>
                <input type="password" id="registerConfirmPassword" placeholder="••••••••" required>
                <span class="password-toggle" onclick="togglePassword('registerConfirmPassword')">👁️</span>
                <div class="error-message" id="registerConfirmError"></div>
            </div>
            <button type="submit" class="btn-submit btn-register">REGISTRIEREN</button>
            <div class="form-toggle">
                Schon einen Account? <a onclick="switchToLogin()">Hier einloggen</a>
            </div>
        </form>
        
        <div class="info-text">
            🔒 Deine Daten sind sicher | SSL verschlüsselt
        </div>
    </div>
</div>

<script>
const users = JSON.parse(localStorage.getItem('users')) || [];
const loginForm = document.getElementById('loginForm');
const registerForm = document.getElementById('registerForm');
const successMessage = document.getElementById('successMessage');

function togglePassword(fieldId) {
    const field = document.getElementById(fieldId);
    if (field.type === 'password') {
        field.type = 'text';
    } else {
        field.type = 'password';
    }
}

function switchToRegister() {
    loginForm.style.display = 'none';
    registerForm.style.display = 'block';
    successMessage.classList.remove('show');
    clearErrors();
}

function switchToLogin() {
    loginForm.style.display = 'block';
    registerForm.style.display = 'none';
    successMessage.classList.remove('show');
    clearErrors();
}

function clearErrors() {
    document.querySelectorAll('.error-message').forEach(el => {
        el.classList.remove('show');
        el.textContent = '';
    });
}

function showError(elementId, message) {
    const element = document.getElementById(elementId);
    element.textContent = message;
    element.classList.add('show');
}

function showSuccess(message) {
    successMessage.textContent = message;
    successMessage.classList.add('show');
    setTimeout(() => {
        successMessage.classList.remove('show');
    }, 4000);
}

// LOGIN Formular
loginForm.addEventListener('submit', function(e) {
    e.preventDefault();
    clearErrors();
    
    const email = document.getElementById('loginEmail').value;
    const password = document.getElementById('loginPassword').value;
    
    if (!email) {
        showError('loginEmailError', 'E-Mail erforderlich');
        return;
    }
    
    if (!password) {
        showError('loginPasswordError', 'Passwort erforderlich');
        return;
    }
    
    const user = users.find(u => u.email === email);
    
    if (!user) {
        showError('loginEmailError', 'E-Mail nicht gefunden');
        return;
    }
    
    if (user.password !== password) {
        showError('loginPasswordError', 'Falsches Passwort');
        return;
    }
    
    showSuccess(`Willkommen zurück, ${user.username}! 🎮`);
    loginForm.reset();
    setTimeout(() => {
        alert(`Login erfolgreich!\nWillkommen ${user.username}!`);
    }, 1500);
});

// REGISTER Formular
registerForm.addEventListener('submit', function(e) {
    e.preventDefault();
    clearErrors();
    
    const username = document.getElementById('registerUsername').value;
    const email = document.getElementById('registerEmail').value;
    const password = document.getElementById('registerPassword').value;
    const confirmPassword = document.getElementById('registerConfirmPassword').value;
    
    let hasError = false;
    
    if (!username || username.length < 3) {
        showError('registerUsernameError', 'Min. 3 Zeichen erforderlich');
        hasError = true;
    }
    
    if (!email || !email.includes('@')) {
        showError('registerEmailError', 'Gültige E-Mail erforderlich');
        hasError = true;
    }
    
    if (users.some(u => u.email === email)) {
        showError('registerEmailError', 'E-Mail bereits registriert');
        hasError = true;
    }
    
    if (!password || password.length < 6) {
        showError('registerPasswordError', 'Min. 6 Zeichen erforderlich');
        hasError = true;
    }
    
    if (password !== confirmPassword) {
        showError('registerConfirmError', 'Passwörter stimmen nicht überein');
        hasError = true;
    }
    
    if (hasError) return;
    
    const newUser = { username, email, password };
    users.push(newUser);
    localStorage.setItem('users', JSON.stringify(users));
    
    showSuccess(`Account erstellt! Willkommen ${username}! 🎉`);
    registerForm.reset();
    
    setTimeout(() => {
        switchToLogin();
        showSuccess(`${username}, du kannst dich nun einloggen!`);
    }, 2000);
});
</script>
</body>
</html>
